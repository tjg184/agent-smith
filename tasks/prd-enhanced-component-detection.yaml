tasks:
  - title: "Story-001: As a user, I want the add-all command to detect all components in a repository so that I can bulk download skills, agents, and commands regardless of how they're organized - System detects SKILL.md files in skills directories (existing behavior preserved), System detects .md files in /agents/ directories as agent components, System detects .md files in /commands/ directories as command components, Bulk download processes all detected component types successfully, Existing functionality remains unchanged for repositories with current structure"
    completed: false
    parallel_group: 0
  
  - title: "Story-002: As a user, I want clear feedback during bulk download so that I know what components were found and their status - Console output shows number of each component type detected, Progress indicators show which components are being downloaded, Success/failure status reported for each component, Summary provided with total counts and any errors encountered"
    completed: false
    parallel_group: 1
  
  - title: "Story-003: As a developer, I want maintainable detection logic so that future repository patterns can be added easily - Detection logic organized by component type with clear separation, Path-based patterns used instead of just filename matching, Commented code explains detection patterns for future modifications, No hard-coded assumptions about directory depth or naming conventions"
    completed: false
    parallel_group: 1
  
  - title: "Implement enhanced detection logic in detectComponentsInRepo function - Modify main.go lines 352-393 to add path-based detection for agents and commands while preserving SKILL.md logic, Use strings.Contains() to match /agents/ and /commands/ paths, Extract component names appropriately (directory for skills, filename for agents/commands)"
    completed: false
    parallel_group: 0
  
  - title: "Update console output in BulkDownloader.AddAll method - Enhance progress feedback to show detection counts by component type, Add individual component status during download processing, Provide comprehensive summary with success/failure statistics"
    completed: false
    parallel_group: 2
  
  - title: "Create unit tests for new detection patterns - Test path-based agent detection, Test path-based command detection, Verify SKILL.md backward compatibility, Test component name extraction for all types, Cover edge cases and error scenarios"
    completed: false
    parallel_group: 2
  
  - title: "Integration testing with wshobson/agents repository - Clone test repository and run add-all command, Verify all components detected and downloaded correctly, Test with existing repositories to ensure no regression, Validate console output format and completeness"
    completed: false
    parallel_group: 2
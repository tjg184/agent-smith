created: "2026-02-04T23:07:00Z"
tasks:
  - title: "Story-001: As a user, I want to install skills with the same name from different repositories so that I can use specialized implementations without conflicts - Lock file nests components by source URL, Installing python-helper from team-a/tools and team-b/tools creates two separate entries, Both installations tracked independently with distinct commit hashes and metadata, Lock file version bumped to v4"
    completed: false
    parallel_group: 0
  - title: "Story-002: As a user, I want to materialize components with the same name from different profiles to the same target so that I can use multiple versions in my project - Materialization metadata nests components by source URL, Materializing python-helper from profile team-a creates entry at skills[sourceURL][python-helper], Materializing same-named component from different profile/source creates separate entry, Materialization metadata version bumped to v2, New field filesystemName tracks actual directory name on disk"
    completed: false
    parallel_group: 0
  - title: "Story-003: As a user, when I materialize a component that conflicts with an existing filesystem name, I want the system to automatically suffix the new component so both can coexist on disk - When materializing python-helper and name already exists on disk use python-helper-2, Subsequent conflicts use python-helper-3 python-helper-4 etc, filesystemName field in metadata stores actual disk name, User informed which filesystem name was used, Display format: Materialized skill 'python-helper' as 'python-helper-2'"
    completed: false
    parallel_group: 1
  - title: "Story-004: As a user, when I reference an ambiguous component name in a command, I want clear error messages guiding me how to disambiguate so I can specify which version I meant - Commands check for ambiguity across all sources, When multiple sources have same component name display error with all matching sources, Error message includes exact command syntax with --source flag, Error message shows source URLs for all matches"
    completed: false
    parallel_group: 1
  - title: "Story-005: As a user, I want to use the --source flag to disambiguate component references so I can operate on the specific version I intend - Add --source url flag to all component commands (update remove materialize), Flag accepts full source URL or short form, When provided lookup uses source URL + component name, Commands operate on single matching component"
    completed: false
    parallel_group: 1
  - title: "Story-006: As a user listing materialized components, I want to see source information for each component so I can distinguish between same-named components from different sources - materialize list displays source URL or profile name for each component, Components with same name clearly differentiated by source, Display includes filesystem name if different from component name"
    completed: false
    parallel_group: 1
  - title: "Story-007: As a developer, I want all lock file read/write operations to use the new nested structure so that the v4 format is consistently applied throughout the codebase - SaveLockFileEntry saves to nested structure, LoadLockFileEntry searches nested structure with source URL + component name, RemoveLockFileEntry removes from nested structure, GetAllComponentNames aggregates names from all sources with duplicate tracking, All operations write lock file as version 4"
    completed: false
    parallel_group: 0
  - title: "Story-008: As a developer, I want all materialization metadata read/write operations to use the new nested structure so that the v2 format is consistently applied throughout the codebase - AddMaterializationEntry saves to nested structure with filesystemName, LoadMaterializationMetadata loads nested structure, GetComponentMap returns nested map structure, GetAllMaterializedComponents iterates nested structure returning flat list with source info, CheckMultipleComponentsSyncStatusBatched handles nested structure, All operations write metadata as version 2"
    completed: false
    parallel_group: 0
  - title: "Story-009: As a developer, I want helper functions to search across all sources for a component name so that ambiguity detection and resolution is centralized - FindComponentSources returns all source URLs with that component, DisambiguateComponent handles single vs multiple matches, Returns error with guidance when multiple sources found and no filter provided, Returns single match when only one source has component, Returns single match when --source filter narrows to one source"
    completed: false
    parallel_group: 0
  - title: "Story-010: As a user, I want the materialize service to resolve filesystem names automatically so that conflicts are handled transparently during materialization - ResolveFilesystemName function checks existing disk names and metadata, Returns base name if no conflict, Returns suffixed name (-2 -3 etc) if conflict exists, Checks both filesystem and materialization metadata for conflicts, Materialization service calls this before copying directory"
    completed: false
    parallel_group: 1
  - title: "Story-011: As a developer updating the materialize service, I want all materialization operations to use nested lookups so that components from different sources are handled correctly - MaterializeComponent uses source-aware lookups, ListMaterialized iterates nested structure, ShowComponentInfo searches by source + name or iterates all sources, ShowStatus handles nested data structure, UpdateMaterialized uses nested lookups and updates"
    completed: false
    parallel_group: 2
  - title: "Story-012: As a developer, I want the profile manager to handle nested lock files when copying components between profiles so that source information is preserved - copyComponentWithMetadata reads from nested source structure, Writes to target profile's nested structure, Preserves source URL and all metadata fields, Handles cases where target already has same component from different source"
    completed: false
    parallel_group: 2
  - title: "Story-013: As a developer, I want the updater service to search nested lock files when checking for updates so that all installed versions can be checked - LoadMetadata searches nested structure, When component name provided without source checks all sources with that name, Returns update status for each source, With --source flag checks only specified source"
    completed: false
    parallel_group: 2
  - title: "Story-014: As a developer, I want the linker service to handle nested lock files when loading component metadata so that linking operations work correctly - loadFromLockFile searches nested structure, Handles ambiguous names by iterating all sources, Uses source information when available for precise lookup"
    completed: false
    parallel_group: 2
  - title: "Story-015: As a user, I want all CLI commands that accept component names to support the --source flag so that I can disambiguate across all operations - Add --source flag to install update remove materialize info commands, Flag is optional only required when ambiguous, Flag accepts full URL or short form, Help text updated to document flag, Example commands in help text show --source usage"
    completed: false
    parallel_group: 2
  - title: "Story-016: As a developer, I want all integration tests updated to use the new lock file and materialization structure so that tests pass with v4/v2 formats - All test fixtures updated to v4 lock file format, All test fixtures updated to v2 materialization format, Tests that create mock lock files use nested structure, Tests that assert on file contents expect nested structure, Tests verify source URL nesting, Tests verify filesystemName field in materialization metadata"
    completed: false
    parallel_group: 2
